<div class="row mb-4">
  <div class="col-md-5">
    <h2> 
      <a href="/post/{{id}}">{{title}}</a>
    </h2>
    <h4>
      {{content}}
    </h4>
    <p>Created by {{user.name}} on {{format_date date_created}}</p>
  </div>
</div>